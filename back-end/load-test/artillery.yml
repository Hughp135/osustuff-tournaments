config:
  target: 'http://localhost:4008/api'
  phases:
    - duration: 30
      arrivalRate: 10
      rampTo: 500
      name: 'Warm up the application'
scenarios:
  - flow:
      - get:
          url: "/lobbies"
